s(z)*s,vy:Math.sin(z)*s,r:(e.type==='boss'||e.type==='harvester')?6:4,l:4,d:e.dmg,c:e.c})","EB.push({x:e.x,y:e.y,vx:Math.cos(z)*s,vy:Math.sin(z)*s,r:(e.type==='boss'||e.type==='harvester')?6:4,l:4,d:e.dmg,c:e.c,src:e.type})",1],["damage(e.dmg);if(e.type==='lancer'&&e.dash>0)e.dash=0","damage(e.dmg,enemyLabel(e.type)+' CONTACT');if(e.type==='lancer'&&e.dash>0)e.dash=0",1],["damage(b.d)}}for(let a of A)","damage(b.d,enemyLabel(b.src)+' PROJECTILE')}}for(let a of A)",1],["damage(g.dmg)}}else g.tick","damage(g.dmg,'GRAVITY WELL')}}else g.tick",1],["damage(18+threatLevel()*2.5);for(let e of E)","damage(18+threatLevel()*2.5,'RIFT FRACTURE');for(let e of E)",1],["$('#earned').textContent=","let dr=$('#deathRecap');if(dr)dr.innerHTML='<b>FAILURE ANALYSIS</b><span>Fatal source: <strong>'+deathCause+'</strong> · '+Math.floor(deathAt)+'s<br>Use the Field Manual if that mechanic is unclear.</span>';$('#earned').textContent=",1],["<button class=go id=again>RUN IT BACK</button><div class=best>","<button class=go id=again>RUN IT BACK</button><div class=row><button class=\"go alt\" id=manualOverBtn>FIELD MANUAL</button></div><div class=best>",1],["$('#manualBtn').onclick=()=>{$('#start').classList.add('hide');$('#manual').classList.remove('hide');guideLabel()};","function openManual(){['start','over'].forEach(id=>$('#'+id).classList.add('hide'));$('#manual').classList.remove('hide');guideLabel()}$('#manualBtn').onclick=openManual;$('#manualOverBtn').onclick=openManual;",1]]