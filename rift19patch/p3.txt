NS');signal('BREACH · 12s','#ff91df',2.3);tone(510,.12);learn('breach','RIFT BREACH','The pink portal core is a real auto-aim target. Destroy it before its countdown ends. Sealing it rewards Credits/Shards; letting it rupture creates a projectile blast, reinforcements and Backlash.') }",1],["function blackoutWave(){blackoutT=9;scanT=Math.max(scanT,1.15);let n=threatLevel()>=4?2:1;for(let i=0;i<n;i++){let a=R(0,6.28),rr=R(125,205);enemy('specter',{x:cl(p.x+Math.cos(a)*rr,28,W-28),y:cl(p.y+Math.sin(a)*rr,72,H-28)})}toast('GRID BLACKOUT','DASH TO PULSE-SCAN · OVERDRIVE ILLUMINATES');signal('BLACKOUT · 9s','#9ab6ff',2.2);burst(p.x,p.y,'#83a9ff',24);shake=Math.max(shake,6);tone(88,.14)}","function blackoutWave(){blackoutT=9;scanT=Math.max(scanT,1.15);let n=threatLevel()>=4?2:1;for(let i=0;i<n;i++){let a=R(0,6.28),rr=R(125,205);enemy('specter',{x:cl(p.x+Math.cos(a)*rr,28,W-28),y:cl(p.y+Math.sin(a)*rr,72,H-28)})}toast('GRID BLACKOUT','DASH TO PULSE-SCAN · OVERDRIVE ILLUMINATES');signal('BLACKOUT · 9s','#9ab6ff',2.2);burst(p.x,p.y,'#83a9ff',24);shake=Math.max(shake,6);tone(88,.14);learn('blackout','GRID BLACKOUT','Darkness also reduces your auto-target range. DASH emits a temporary pulse scan; OVERDRIVE keeps a much larger area visible. Survive until power returns. Specters are the teleporting blue enemies associated with Blackouts.') }",1],["function bossSpoils(){state=5;U.rk.textContent='MAJOR TARGET DESTROYED';","function bossSpoils(){if(guideOn&&!learned.spoils&&state==1){learned.spoils=1;ss('rrlearned',JSON.stringify(learned))}state=5;U.rk.textContent='MAJOR TARGET DESTROYED';",1],["U.rs.textContent='POWER NOW · PROGRESSION LATER · OR FEED THE CORE';","U.rs.textContent='RELIC = POWER · SALVAGE = META PROGRESSION · CORE = SHARDS + BACKLASH';",1],["toast('RELIC BONDED',r[0]+' · THREAT '+threatLevel());burst(p.x,p.y,'#c17cff',34);tone(1180,.16)","toast('RELIC BONDED',r[0]+' · THREAT '+threatLevel());burst(p.x,p.y,'#c17cff',34);tone(1180,.16);learn('relic','RELICS & STRAIN','Relics are rare run-only upgrades and are much stronger than normal Evolutions. Your first two are strain-free. From Relic #3 onward, Relic Strain reduces healing, increases incoming damage and pushes Threat upward faster.')",1],["enemy('devourer');feederAt+=58;toast('HUNGER SIGNAL','DEVOURER TRACKING LOOSE SHARDS');signal('DEVOURER INBOUND','#ffd078',2.1);tone(190,.1)","enemy('devourer');feederAt+=58;toast('HUNGER SIGNAL','DEVOURER TRACKING LOOSE SHARDS');signal('DEVOURER INBOUND','#ffd078',2.1);tone(190,.1);learn('hunger','HUNGER = THE DEVOURER','HUNGER is not a meter you manage. It means a Devourer has entered the run. It hunts loose cyan Shards first. Each Shard it eats powers it up. At 3+ stolen Shard-weight it becomes GORGED and hunts you. Killing it returns the stolen value and banks +1 Credit.')",1],["if(tl!==lastThreat){if(tl>lastThreat&&tl>0)signal('THREAT '+tl+' · '+threatName(tl),tl>=5?'#ff657f':tl>=3?'#ffba72':'#8ff3ff',2.4);lastThreat=tl}","if(tl!==lastThreat){if(tl>lastThreat&&tl>0){signal('THREAT '+tl+' · '+threatName(tl),tl>=5?'#ff657f':tl>=3?'#ffba72':'#8ff3ff',2.4);learn('threat','THREAT LEVEL','T# beside the timer is the Rift reacting to your run. More survival time and powerful Relic stacks raise Threat. Higher Threat increases enemy durability, damage, speed, elite pressure and score value. It is difficulty escalation, not another resource you spend.')}lastThreat=tl}",1],["function damage(d){if(p.iv>0||state!=1)return;if(p.nullMantle&&p.nullCd<=0){p.nullCd=18;p.iv=.32;shake=5;burst(p.x,p.y,'#c8a8ff',20);toast('NULL MANTLE','HIT ABSORBED');tone(420,.08);return}d*=1+strain()*.04;p.hp-=d;p.traumaT=4.2;p.iv=.48;combo=Math.max(1,combo*.55);hot=0;shake=9;burst(p.x,p.y,'#ff5d7c',13);tone(120,.08);if(p.hp<=0)end()}","function damage(d,src='RIFT DAMAGE'){if(p.iv>0||state!=1)return;if(p.nullMantle&&p.nullCd<=0){p.nullCd=18;p.iv=.32;shake=5;burst(p.x,p.y,'#c8a8ff',20);toast('NULL MANTLE','HIT ABSORBED');tone(420,.08);return}deathCause=src;deathAt=run;d*=1+strain()*.04;p.hp-=d;p.traumaT=4.2;p.iv=.48;combo=Math.max(1,combo*.55);hot=0;shake=9;burst(p.x,p.y,'#ff5d7c',13);tone(120,.08);if(p.hp<=0)end()}",1],["EB.push({x:e.x,y:e.y,vx:Math.co