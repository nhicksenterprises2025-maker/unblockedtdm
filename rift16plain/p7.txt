x(0,shake-dt*35);p.iv=Math.max(0,p.iv-dt);if(overT>0)overActive+=dt;overT=Math.max(0,overT-dt);overLock=Math.max(0,overLock-dt);overSat=Math.max(0,overSat-dt*.72);if(overCharge>=100&&overT<=0&&overLock<=0){overReady+=dt;if(overReady>8)overCharge=Math.max(88,overCharge-dt*3)}else overReady=0;surgeT=Math.max(0,surgeT-dt);backlashT=Math.max(0,backlashT-dt);bossGrace=Math.max(0,bossGrace-dt);p.traumaT=Math.max(0,p.traumaT-dt);p.siphonT=Math.max(0,p.siphonT-dt);p.repairPool=Math.min(12,p.repairPool+dt*2.4);p.nullCd=Math.max(0,(p.nullCd||0)-dt);p.cd-=dt;if(hot>0)hot=Math.max(0,hot-dt*.35);else combo=Math.max(1,combo-dt*.28);if(p.dashes<p.maxDash){p.re+=dt;if(p.re>p.reTime){p.re=0;p.dashes++}}if(p.dashT>0){p.dashT-=dt;p.x+=p.vx*dt;p.y+=p.vy*dt;for(let e of E)if(!e.dead&&ds(p,e)<(p.r+e.r+8)**2)hit(e,95+p.level*6,1)}else{let sm=overT>0?1.18:1;p.x+=mv[0]*p.spd*sm*dt;p.y+=mv[1]*p.spd*sm*dt}p.x=cl(p.x,18,W-18);p.y=cl(p.y,55,H-18);if(p.cd<=0)fire();spawn-=dt;if(spawn<0){if(bossGrace>0){spawn=.18}else{let q=Math.random(),ty=run>120&&q<.06?'shade':run>55&&q<.12?'harrier':run>24&&q<.18?'lancer':run>80&&q<.25?'siphon':run>60&&q<.33?'aegis':run>95&&q<.40?'bomber':run>70&&q<.49?'sniper':run>50&&q<.59?'tank':run>35&&q<.69?'splitter':run>12&&q<.81?'spitter':'chaser';enemy(ty);spawn=Math.max(.19,.76-run*.0038)*R(.75,1.15)*(surgeT>0?.68:1)*(backlashT>0?.70:1)*(1-Math.min(.22,threatLevel()*.035))}}if(!ascended&&run>=ascendAt){ascended=1;ascensionChoice();return}if(!breakpoint&&run>=50){breakpoint=1;breaks++;breakBonusC=2;breakBonusX=2;score+=1200;ss('rrbreaks',breaks);meta();toast('BREAKPOINT CLEARED','50s · +2 CREDITS · +2 MASTERY BANKED');tone(1120,.15)}if(run>=anchorAt&&!A.length){anchorWave();anchorAt+=42}if(run>=breachAt&&!E.some(e=>(e.type==='boss'||e.type==='harvester'||e.type==='breach')&&!e.dead)){breachWave();breachAt+=52}if(run>=fractureAt){fractureWave();fractureAt+=34}if(run>=surgeAt){surgeT=11;surgeAt+=75;toast('RIFT SURGE','11s · MORE THREATS · +35% OVERDRIVE CHARGE');shake=7}if(run>=hunterAt){enemy('hunter');hunterAt+=60;toast('HUNTER SIGNAL','RIFT STALKER INBOUND');shake=6}if(run>=bossAt){EB.length=0;G.length=0;bossGrace=2.35;let bt=bossCycle++%2?'harvester':'boss';enemy(bt);bossAt+=60;toast(bt==='boss'?'WARDEN ARRIVAL':'NULL HARVESTER',bt==='boss'?'PROJECTILES PURGED · READ THE TELEGRAPHS':'GRAVITY SIGNATURE · KEEP MOVING');shake=8}if(run>=nemesisAt&&threatLevel()>=3&&!E.some(e=>(e.type==='nemesis'||e.type==='boss'||e.type==='harvester')&&!e.dead)){enemy('nemesis');nemesisAt+=95;toast('NEMESIS LOCK','THE RIFT IS ADAPTING TO YOUR BUILD');shake=10}for(let e of E)if(!e.dead){e.hitT=Math.max(0,(e.hitT||0)-dt);enemyUpdate(e,dt)}for(let b of B){b.px=b.x;b.py=b.y;b.x+=b.vx*dt;b.y+=b.vy*dt;b.l-=dt;for(let e of E)if(!e.dead&&b.l>0&&segHit(b,e)){hit(e,b.d);burst(b.x,b.y,b.crit?'#ffe58a':'#b7fbff',3);tone(b.crit?760:540,.025);if(--b.pi<0)b.l=0}}for(let b of EB){b.x+=b.vx*dt;b.y+=b.vy*dt;b.l-=dt;if(b.l>0&&ds(p,b)<(p.r+b.r)**2){b.l=0;damage(b.d)}}for(let a of A)anchorUpdate(a,dt);clean(A,a=>!a.dead);for(let g of G){g.life-=dt;let gd=Math.sqrt(ds(p,g));if(gd<g.r){let ga=Math.atan2(g.y-p.y,g.x-p.x),gf=(1-gd/g.r)*g.pull;p.x+=Math.cos(ga)*gf*dt;p.y+=Math.sin(ga)*gf*dt;g.tick-=dt;if(g.tick<=0){g.tick=.72;damage(g.dmg)}}else g.tick=Math.max(0,g.tick-dt)}clean(G,g=>g.life>0);p.x=cl(p.x,18,W-18);p.y=cl(p.y,55,H-18);for(let f of F){if(!f.boom){f.t-=dt;if(f.t<=0){f.boom=1;f.l=.38;shake=Math.max(shake,8);burst(f.x,f.y,'#ff4d72',28);tone(115,.08);if(ds(p,f)<f.r*f.r)damage(18+threatLevel()*2.5);for(let e of E)if(!e.dead&&ds(e,f)<f.r*f.r)hit(e,70+threatLevel()*12,1)}}else f.l-=dt}clean(F,f=>!f.boom||f.l>0);for(let q of Q){q.x+=q.vx*dt;q.y+=q.vy*dt;q.vx*=.95;q.vy*=.95;let d=Math.sqrt(ds(p,q));if(d<p.mag){let a=Math.atan2(p.y-q.y,p.x-q.x),z=(1-d/p.mag)*550;q.vx+=Math.cos(a)*z*dt;q.vy+=Math.sin(a)*z*dt}if(d<24){q.dead=1;p.shards+=q.w;score+=20*q.w;if(p.shards>=p.next&&state==1)evolve()}}for(let z of P){z.x+=z.vx*dt;z.y+=z.vy*dt;z.vx*=.97;z.vy*=.97;z.l-=dt}clean(E,e=>!e.dead);clean(B,b=>b.l>0);clean(EB,b=>b.l>0);clean(Q,q=>!q.dead);clean(P,z=>z.l>0);if(P.length>420)P.splice(0,P.length-420);if(EB.length>260)EB.splice(0,EB.length-260);if(B.length>220)B.splice(0,B.length-220);if(G.length>9)G.splice(0,G.length-9);U.s.textContent=Math.floor(score).toLocaleString();let tl=threatLevel();U.t.textContent=String(Math.floor(run/60)).padStart(2,'0')+':'+String(Math.floor(run%60)).padStart(2,'0')+' · T'+tl;U.h.style.width=(overT>0?100:overCharge)+'%';U.c.textContent='CORE '+p.level+' · '+p.shards+'/'+p.next;U.m.textContent=combo>1.8?'x'+combo.toFixed(1)+' COMBO':'';U.w.textContent=p.weaponName+' · MASTERY '+p.masteryRank+'/10';if(U.dir)U.dir.textContent=directiveText();U.d.textContent='●'.repeat(p.dashes)+'○'.repeat(p.maxDash-p.dashes);U.th.textContent='THREAT '+tl+' · '+threatName(tl);let autoMsg='',autoColor='#8ff3ff';if(p.siphonT>0){autoMsg='SIPHONED · REPAIRS SUPPRESSED';autoColor='#ff8f9e'}else if(p.traumaT>0){autoMsg='TRAUMA · '+p.traumaT.toFixed(1)+'s';autoColor='#ff8f9e'}else if(backlashT>0){autoMsg='BACKLASH · '+backlashT.toFixed(1)+'s';autoColor='#ff9d72'}else if(surgeT>0){autoMsg='RIFT SURGE · '+surgeT.toFixed(0)+'s';autoColor='#7adfff'}else if(strain()>=2){autoMsg='RELIC STRAIN '+strain();autoColor='#d6a4ff'}if(tl!==lastThreat){if(tl>lastThreat&&tl>0)signal('THREAT '+tl+' · '+threatName(tl),tl>=5?'#ff657f':tl>=3?'#ffba72':'#8ff3ff',2.4);lastThreat=tl}signalT=Math.max(0,signalT-dt);let showMsg=autoMsg||(signalT>0?signalMsg:'');U.sig.textContent=showMsg;U.sig.style.color=autoMsg?autoColor:signalColor;U.sig.classList.toggle('show',!!showMsg);U.st.textContent=autoMsg;U.ot.textContent=overT>0?overT.toFixed(1)+'s':overLock>0?'LOCK '+overLock.toFixed(1)+'s':overCharge>=100?'READY':Math.floor(overCharge)+'%';U.o.classList.toggle('ready',overCharge>=100&&overT<=0&&overLock<=0);U.o.classList.toggle('active',overT>0);U.o.classList.toggle('locked',overLock>0&&overT<=0);let bo=E.find(e=>(e.type==='boss'||e.type==='harvester')&&!e.dead)||E.find(e=>e.type==='nemesis'&&!e.dead)||E.find(e=>e.type==='hunter'&&!e.dead);if(bo){U.boss.classList.add('show');U.bf.style.width=cl(bo.hp/bo.max*100,0,100)+'%';U.bn.textContent=bo.type==='hunter'?'RIFT STALKER':bo.type==='nemesis'?('RIFT NEMESIS'+(bo.phase?' · ADAPTED':'')):bo.type==='harvester'?('NULL HARVESTER'+(bo.intro>0?' · ARRIVING':bo.phase>=2?' · DEVOUR