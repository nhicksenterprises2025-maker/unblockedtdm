.blink-=dt;let orbit=Math.sin(run*3.4+e.x*.02)*(enr?.9:.65),ang=a+orbit;e.x+=Math.cos(ang)*e.spd*dt;e.y+=Math.sin(ang)*e.spd*dt;if(e.shoot<0){bullet(e,a,enr?330:290,enr?5:3);e.shoot=enr?.48:.68}if(e.rad<0){let n=enr?14:10;for(let i=0;i<n;i++)bullet(e,i/n*6.28,enr?215:180,1);e.rad=enr?1.45:2.15}if(e.blink<0){let z=a+Math.PI+R(-.8,.8),rr=R(120,190);burst(e.x,e.y,'#ff365f',16);e.x=cl(p.x+Math.cos(z)*rr,28,W-28);e.y=cl(p.y+Math.sin(z)*rr,72,H-28);burst(e.x,e.y,'#ff365f',16);e.blink=enr?1.8:2.7}}else if(e.type==='boss'){if(e.leech&&d<330)p.siphonT=.32;if(e.intro>0){e.intro-=dt;let z=d>245?.42:0;e.x+=Math.cos(a)*e.spd*z*dt;e.y+=Math.sin(a)*e.spd*z*dt}else{let enr=e.hp/e.max<.5,rupt=e.hp/e.max<.25&&threatLevel()>=4;if(enr&&!e.enrage){e.enrage=1;toast('WARDEN ENRAGED','PHASE TWO');shake=10}if(rupt&&!e.rupture){e.rupture=1;toast('WARDEN RUPTURE','PHASE THREE · THREAT RESPONSE');shake=13}let z=d>(rupt?170:enr?195:225)?1:d<145?-1:0;e.x+=Math.cos(a)*e.spd*(rupt?1.16:1)*z*dt;e.y+=Math.sin(a)*e.spd*(rupt?1.16:1)*z*dt;e.shoot-=dt;e.rad-=dt;if(e.aimT>0){e.aimT-=dt;if(e.aimT<=0){bullet(e,e.aimA,e.pendingS,e.pendingN);tone(210,.04)}}else if(e.shoot<0){let n=rupt?10:enr?7:5;if(e.mod==='FRENZIED')n+=2;e.pendingN=n;e.pendingS=rupt?300:enr?250:220;e.aimA=a;e.aimT=rupt?.28:enr?.38:.58;e.shoot=rupt?.55:e.mod==='FRENZIED'?(enr?.62:.78):(enr?.78:1.05)}if(e.novaT>0){e.novaT-=dt;if(e.novaT<=0){let n=(rupt?22:enr?16:12)+(e.mod==='VOIDBORN'?6:0);for(let i=0;i<n;i++)bullet(e,i/n*6.28,e.mod==='VOIDBORN'?(rupt?235:180):(rupt?205:150),1);tone(130,.06)}}else if(e.rad<0){e.novaT=rupt?.32:enr?.48:.70;e.rad=rupt?1.48:e.mod==='VOIDBORN'?(enr?1.78:2.15):(enr?2.55:3.35)}}}else{e.x+=Math.cos(a)*e.spd*dt;e.y+=Math.sin(a)*e.spd*dt}if(e.type!=='breach'&&ds(p,e)<(p.r+e.r)**2){damage(e.dmg);if(e.type==='lancer'&&e.dash>0)e.dash=0}}function end(){state=4;let earned=Math.max(1,Math.floor(score/1800)+Math.floor(kills/12)+Math.floor(run/45)+breakBonusC+anchorBonusC+breachBonusC),mxp=Math.max(1,Math.floor(kills/3)+Math.floor(run/10)+Math.floor(peakThreat/2)+(breakpoint?2:0)+directiveBonus),oldRank=masteryRank(p.weapon);mastery[p.weapon]=(mastery[p.weapon]||0)+mxp;masteryBest[p.weapon]=Math.max(masteryBest[p.weapon]||0,run);ss('rrmastery',JSON.stringify(mastery));ss('rrmasteryBest',JSON.stringify(masteryBest));let newRank=masteryRank(p.weapon);credits+=earned;best=Math.max(best,Math.floor(score));bestTime=Math.max(bestTime,run);bestKills=Math.max(bestKills,kills);ss('rrbest',best);ss('rrbesttime',bestTime);ss('rrbestkills',bestKills);ss('rrcredits',credits);meta();$('#final').textContent=Math.floor(score).toLocaleString();$('#stats').textContent=Math.floor(run)+'s survived · '+kills+' kills · max x'+maxCombo.toFixed(1)+' · '+p.weaponName+' · Core Mk '+p.level+' · '+overUses+' Overdrive ('+Math.round(overActive/Math.max(1,run)*100)+'% uptime) · '+p.relics.length+' Relics · Threat '+peakThreat+(strain()?' · Strain '+strain():'')+(p.ascension?' · '+p.ascension:'')+(breakpoint?' · BREAKPOINT CLEARED':'')+(directiveDone?' · DIRECTIVE COMPLETE':'')+(anchorsCaptured||anchorsLost?' · '+anchorsCaptured+' ANCHORS SECURED · '+anchorsLost+' RUPTURED':'')+(breachesClosed||breachesRuptured?' · '+breachesClosed+' BREACHES SEALED · '+breachesRuptured+' OPENED':'')+(blackouts?' · '+blackouts+' BLACKOUTS SURVIVED':'');$('#earned').textContent='+'+earned+' RIFT CREDITS · +'+mxp+' '+p.weaponName+' XP'+(directiveDone?' · +3 DIRECTIVE':'')+(newRank>oldRank?' · MASTERY RANK '+newRank:'');$('#best2').textContent=best.toLocaleString();$('#over').classList.remove('hide')}function clean(a,f){let w=0;for(let v of a)if(f(v))a[w++]=v;a.length=w}function segHit(b,e){let vx=b.x-b.px,vy=b.y-b.py,wx=e.x-b.px,wy=e.y-b.py,d=vx*vx+vy*vy,t=d?cl((wx*vx+wy*vy)/d,0,1):0,dx=b.px+vx*t-e.x,dy=b.py+vy*t-e.y,r=(b.r||4)+e.r;return dx*dx+dy*dy<r*r}function update(dt){run+=dt;updateDirective();peakThreat=Math.max(peakThreat,threatLevel());score+=dt*(7+run*.05)*combo*(surgeT>0?1.45:1)*(1+threatLevel()*.08);shake=Math.max(0,shake-dt*35);p.iv=Math.max(0,p.iv-dt);if(overT>0)overActive+=dt;overT=Math.max(0,overT-dt);overLock=Math.max(0,overLock-dt);overSat=Math.max(0,overSat-dt*.72);if(overCharge>=100&&overT<=0&&overLock<=0){overReady+=dt;if(overReady>8)overCharge=Math.max(88,overCharge-dt*3)}else overReady=0;surgeT=Math.max(0,surgeT-dt);backlashT=Math.max(0,backlashT-dt);let wasBlack=blackoutT>0;blackoutT=Math.max(0,blackoutT-dt);scanT=Math.max(0,scanT-dt);if(wasBlack&&blackoutT<=0){blackouts++;score+=600;signal('POWER RESTORED · +600 SCORE','#8ff3ff',2.1);tone(820,.09)}bossGrace=Math.max(0,bossGrace-dt);p.traumaT=Math.max(0,p.traumaT-dt);p.siphonT=Math.max(0,p.siphonT-dt);p.repairPool=Math.min(12,p.repairPool+dt*2.4);p.nullCd=Math.max(0,(p.nullCd||0)-dt);p.cd-=dt;if(hot>0)hot=Math.max(0,hot-dt*.35);else combo=Math.max(1,combo-dt*.28);if(p.dashes<p.maxDash){p.re+=dt;if(p.re>p.reTime){p.re=0;p.dashes++}}if(p.dashT>0){p.dashT-=dt;p.x+=p.vx*dt;p.y+=p.vy*dt;for(let e of E)if(!e.dead&&ds(p,e)<(p.r+e.r+8)**2)hit(e,95+p.level*6,1)}else{let sm=overT>0?1.18:1;p.x+=mv[0]*p.spd*sm*dt;p.y+=mv[1]*p.spd*sm*dt}p.x=cl(p.x,18,W-18);p.y=cl(p.y,55,H-18);if(p.cd<=0)fire();spawn-=dt;if(spawn<0){if(bossGrace>0){spawn=.18}else{let q=Math.random(),ty=blackoutT>0&&q<.12?'specter':run>120&&q<.06?'shade':run>55&&q<.12?'harrier':run>24&&q<.18?'lancer':run>80&&q<.25?'siphon':run>60&&q<.33?'aegis':run>95&&q<.40?'bomber':run>70&&q<.49?'sniper':run>50&&q<.59?'tank':run>35&&q<.69?'splitter':run>12&&q<.81?'spitter':'chaser';enemy(ty);spawn=Math.max(.19,.76-run*.0038)*R(.75,1.15)*(surgeT>0?.68:1)*(backlashT>0?.70:1)*(1-Math.min(.22,threatLevel()*.035))}}if(!ascended&&run>=ascendAt){ascended=1;ascensionChoice();return}if(!breakpoint&&run>=50){breakpoint=1;breaks++;breakBonusC=2;breakBonusX=2;score+=1200;ss('rrbreaks',breaks);meta();toast('BREAKPOINT CLEARED','50s · +2 CREDITS · +2 MASTERY BANKED');tone(1120,.15)}if(run>=anchorAt&&!A.length){anchorWave();anchorAt+=42}if(run>=breachAt&&!E.some(e=>(e.type==='boss'||e.type==='harv