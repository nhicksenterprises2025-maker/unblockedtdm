0,dmg:28,shots:1,pierce:0,level:1,shards:0,next:7,dashes:3,maxDash:3,re:0,reTime:2.8,dashT:0,iv:0,vx:0,vy:0,mag:130,heal:0,repairPool:12,traumaT:0,siphonT:0,vamp:0,crit:.05,critMult:1.8,perks:[],weapon:'pulse',weaponName:'PULSE CARBINE',proj:535,spread:.14,bulletR:4,weaponColor:'#b7fbff',overDur:6.2,overGain:1,overLockBase:8,volt:0,shieldBonus:1,relics:[],dmgMult:1,echo:0,dashBomb:0,nullMantle:0,nullCd:0,berserk:0,execute:0,ascension:'',masteryRank:0};const directiveDefs=[{id:'survive',name:'SURVIVE 45s',target:45,now:()=>Math.floor(run)},{id:'kills',name:'ELIMINATE 30',target:30,now:()=>kills},{id:'core',name:'REACH CORE Mk 4',target:4,now:()=>p.level},{id:'over',name:'ACTIVATE OVERDRIVE',target:1,now:()=>overUses}];function chooseDirective(){directive=directiveDefs[Math.floor(Math.random()*directiveDefs.length)];directiveDone=0;directiveBonus=0;if(U.dir)U.dir.textContent='MASTERY DIRECTIVE · '+directive.name+' · +3 XP'}function updateDirective(){if(!directive||directiveDone)return;let v=directive.now();if(v>=directive.target){directiveDone=1;directiveBonus=3;toast('MASTERY DIRECTIVE COMPLETE','+3 '+p.weaponName+' XP BANKED');tone(980,.12)}}function directiveText(){if(!directive)return'MASTERY DIRECTIVE · NONE';if(directiveDone)return'MASTERY DIRECTIVE · COMPLETE · +3 XP';let v=Math.min(directive.target,directive.now());return'MASTERY DIRECTIVE · '+directive.name+' · '+v+'/'+directive.target}function strain(){return Math.max(0,p.relics.length-2)}function healEff(){return cl((p.traumaT>0?.55:1)*(p.siphonT>0?.35:1)*(1-strain()*.08),.22,1)}function repair(a,limited=0){if(a<=0||p.hp<=0)return 0;let v=a*healEff();if(limited){v=Math.min(v,p.repairPool);p.repairPool=Math.max(0,p.repairPool-v)}let old=p.hp;p.hp=Math.min(p.max,p.hp+v);return p.hp-old}function threatLevel(){return cl(p.relics.length+Math.floor(Math.max(0,run-55)/75)+Math.floor(strain()/2),0,6)}function threatName(v=threatLevel()){return ['CALM','ALERT','HOSTILE','HUNT','SEVERE','CRITICAL','APOCALYPSE'][v]||'APOCALYPSE'}function size(){D=Math.min(devicePixelRatio||1,2);W=innerWidth;H=innerHeight;C.width=W*D;C.height=H*D}addEventListener('resize',size);size();function audio(){try{return audio.a||(audio.a=new(AudioContext||webkitAudioContext))}catch(e){}}function tone(f=500,d=.05){let a=audio();if(!a)return;try{a.resume();let o=a.createOscillator(),g=a.createGain();o.frequency.value=f;g.gain.value=.022;o.connect(g).connect(a.destination);o.start();g.gain.exponentialRampToValueAtTime(.001,a.currentTime+d);o.stop(a.currentTime+d)}catch(e){}}function toast(a,b=''){U.toast.innerHTML=a+(b?'<small>'+b+'</small>':'');U.toast.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>U.toast.classList.remove('show'),1200)}function burst(a,b,c,n=10){for(let i=0;i<n;i++){let z=R(0,6.28),v=R(35,150);P.push({x:a,y:b,vx:Math.cos(z)*v,vy:Math.sin(z)*v,l:.45,c})}}function reset(){E.length=B.length=EB.length=Q.length=P.length=F.length=0;run=score=kills=hot=shake=overCharge=overT=overUses=overLock=overSat=overReady=overActive=surgeT=relicDrops=backlashT=peakThreat=bossGrace=breakpoint=breakBonusC=breakBonusX=directiveDone=directiveBonus=0;directive=null;combo=maxCombo=1;spawn=0;bossAt=45;hunterAt=27;surgeAt=75;nemesisAt=105;fractureAt=40;ascendAt=30;ascended=0;Object.assign(p,{x:W/2,y:H*.62,max:100+hullHp(mods.hull||0),spd:210*(1+modPct(mods.thrusters||0)),fire:.18,cd:0,dmg:28*(1+modPct(mods.reactor||0)),shots:1,pierce:0,level:1,shards:0,next:7,dashes:3,maxDash:3,re:0,reTime:2.8*(1-modPct(mods.capacitor||0)),dashT:0,iv:0,mag:130,heal:0,repairPool:12,traumaT:0,siphonT:0,vamp:0,crit:.05,critMult:1.8,perks:[],weapon:selectedWeapon,overDur:6.2,overGain:1,overLockBase:8,volt:0,shieldBonus:1,relics:[],dmgMult:1,echo:0,dashBomb:0,nullMantle:0,nullCd:0,berserk:0,execute:0,ascension:'',masteryRank:0});applyWeapon(selectedWeapon);chooseDirective();p.hp=p.max;U.rh.textContent='RELICS · 0';U.th.textContent='THREAT 0 · CALM';U.st.textContent='REPAIR SYSTEM · NOMINAL';U.boss.classList.remove('show')}const weaponDefs={pulse:{name:'PULSE CARBINE',tag:'BALANCED',desc:'Fast, precise pulses with clean scaling.',dmg:28,fire:.18,shots:1,pierce:0,proj:535,spread:.14,r:4,color:'#b7fbff'},scatter:{name:'SCATTER DRIVER',tag:'CLOSE BURST',desc:'Five low-damage shards. Devastating when the swarm closes in.',dmg:12,fire:.42,shots:5,pierce:0,proj:470,spread:.20,r:4,color:'#ffbd7a'},rail:{name:'RAIL LANCE',tag:'HEAVY PIERCE',desc:'Slow hypervelocity bolts with massive damage and penetration.',dmg:64,fire:.60,shots:1,pierce:3,proj:760,spread:.04,r:5,color:'#d49cff'}};function applyWeapon(k){let w=weaponDefs[k]||weaponDefs.pulse,r=masteryRank(k);p.weapon=k;p.weaponName=w.name;p.masteryRank=r;p.dmg=w.dmg*(1+modPct(mods.reactor||0))*(1+masteryDamagePct(r)/100);p.fire=w.fire;p.shots=w.shots;p.pierce=w.pierce;p.proj=w.proj;p.spread=w.spread;p.bulletR=w.r;p.weaponColor=w.color;U.w.textContent=w.name+' · '+w.tag}function renderArmory(){let root=$('#weaponCards');root.innerHTML='';for(let k in weaponDefs){let w=weaponDefs[k],mi=masteryInfo(k),b=document.createElement('button');b.className='card weapon-card'+(selectedWeapon===k?' selected':'');let next=mi.r>=10?'MASTERY MAX · +'+masteryDamagePct(10).toFixed(1)+'% TOTAL WEAPON DAMAGE':'NEXT RANK '+(mi.r+1)+' · '+mi.target+' TOTAL XP · '+mi.remain+' XP LEFT'+(mi.gate?' · '+(mi.gateDone?'SURVIVAL GATE CLEARED':'SURVIVE '+mi.gate+'s WITH THIS WEAPON'):'')+' · +'+masteryDamagePct(mi.r+1).toFixed(1)+'% TOTAL DAMAGE';b.innerHTML='<b>'+w.name+'</b><span>'+w.desc+'</span><strong>DMG '+w.dmg+' · '+(1/w.fire).toFixed(1)+'/s'+(w.pierce?' · PIERCE '+w.pierce:'')+' · MASTERY '+mi.r+'/10</strong><span>CURRENT XP · '+mi.xp+(mi.r<10?' / '+mi.target:' · MAX')+' · BEST RUN '+Math.floor(mi.bt)+'s · DAMAGE +'+masteryDamagePct(mi.r).toFixed(1)+'%</span><div class=mastery-track><i style=width:'+mi.pct.toFixed(1)+'%></i></div><span class=mastery-next>'+next+'</span><em>'+w.tag+'</em>';b.onpointerdown=e=>{e.preventDefault();selectedWeapon=k;localStorage.setItem('rrweapon',k);start()};root.appendChild(b)}}function openArmory(){renderArmory();$('#start').classList.add('hide');$('#over').classList.add('hide');$('#armory').classList.remove('hide')}function start(){reset();state=1;$('#start').classList.add('hide');$('#over').classList.add('hide');$('#armory').classList.add('hide');U.up.classList.add('hide');last=performance.now();tone(260,.12);requestAnimationFrame(loop)}$('#go').onclick=$('#again').onclick=openArmory;$('#armoryBack').onclick=()=>{$(