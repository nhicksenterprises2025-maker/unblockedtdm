t w=Math.max(1,tq.w||1);tq.dead=1;e.stolen+=w;shardsStolen+=w;e.feed=Math.min(6,e.feed+w);e.max+=18*w;e.hp=Math.min(e.max,e.hp+32*w);e.dmg+=1.7*w;e.spd=Math.min(142,e.spd+2.8*w);e.r=Math.min(24,e.r+1.15*w);e.value+=90*w;burst(e.x,e.y,'#ffd06c',12+Math.min(16,w*3));tone(260+e.feed*45,.045);if(e.feed>=3){signal('DEVOURER GORGED · HUNTING YOU','#ffbd65',2);toast('DEVOURER GORGED','KILL IT TO RECOVER SHARDS + CREDIT');shake=Math.max(shake,6);e.rad=.9}}}else{let oa=a+Math.PI/2*Math.sign(e.orbit||1),z=e.feed>=3?(d>135?1:.35):(d>210?1:d<145?-.4:.12);e.x+=(Math.cos(a)*z+Math.cos(oa)*(e.feed>=3?.42:.28))*e.spd*(e.feed>=3?1.12:1)*dt;e.y+=(Math.sin(a)*z+Math.sin(oa)*(e.feed>=3?.42:.28))*e.spd*(e.feed>=3?1.12:1)*dt;if(e.feed>=3&&e.rad<=0&&d<360){let n=8+Math.min(4,e.feed);for(let i=0;i<n;i++)bullet(e,i/n*6.283,205+e.feed*7,1);e.rad=Math.max(1.55,2.35-e.feed*.09);tone(170,.05)}}}else if(e.type==='aegis'){let z=d>115?1:.45;e.x+=Math.cos(a)*e.spd*z*dt;e.y+=Math.sin(a)*e.spd*z*dt}else if(e.type==='specter'){e.shoot-=dt;e.blink-=dt;let oa=a+Math.PI/2*Math.sign(e.orbit||1),z=d>205?1:d<125?-.72:.08;e.x+=(Math.cos(a)*z+Math.cos(oa)*.58)*e.spd*dt;e.y+=(Math.sin(a)*z+Math.sin(oa)*.58)*e.spd*dt;if(e.shoot<0){bullet(e,a,265,1);e.shoot=1.55}if(e.blink<0){let q=a+Math.PI+R(-1.0,1.0),rr=R(105,175);burst(e.x,e.y,'#79a8ff',10);e.x=cl(p.x+Math.cos(q)*rr,24,W-24);e.y=cl(p.y+Math.sin(q)*rr,70,H-24);burst(e.x,e.y,'#79a8ff',10);e.blink=R(1.8,2.6)}}else if(e.type==='shade'){e.blink-=dt;e.x+=Math.cos(a)*e.spd*dt;e.y+=Math.sin(a)*e.spd*dt;if(e.blink<0){let z=R(0,6.28),rr=R(95,165);burst(e.x,e.y,'#8b72ff',12);e.x=cl(p.x+Math.cos(z)*rr,24,W-24);e.y=cl(p.y+Math.sin(z)*rr,70,H-24);burst(e.x,e.y,'#8b72ff',12);e.blink=R(1.7,2.5)}}else if(e.type==='lancer'){if(e.dash>0){e.dash-=dt;e.x+=e.lx*dt;e.y+=e.ly*dt}else if(e.wind>0){e.wind-=dt;if(e.wind<=0){let aa=Math.atan2(p.y-e.y,p.x-e.x);e.lx=Math.cos(aa)*420;e.ly=Math.sin(aa)*420;e.dash=.42;tone(180,.05)}}else{e.cool-=dt;let z=d>150?1:.25;e.x+=Math.cos(a)*e.spd*z