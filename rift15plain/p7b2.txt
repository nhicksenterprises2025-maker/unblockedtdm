y,e.r*.36,0,7);x.fill();x.shadowBlur=0;if(e.hitT>0&&e.type!=='boss'){let bw=e.r*2.2,bx=e.x-bw/2,by=e.y-e.r-10;x.fillStyle='#ffffff24';x.fillRect(bx,by,bw,3);x.fillStyle='#8af4ff';x.fillRect(bx,by,bw*cl(e.hp/e.max,0,1),3)}}for(let z of P){x.globalAlpha=Math.max(0,z.l/.45);x.fillStyle=z.c;x.beginPath();x.arc(z.x,z.y,2,0,7);x.fill()}if(p.relics.length){x.strokeStyle='#ba7cff';x.lineWidth=1.5;x.globalAlpha=.25+.12*Math.sin(run*5);x.beginPath();x.arc(p.x,p.y,p.r+14+p.relics.length,0,7);x.stroke();x.globalAlpha=1}if(overT>0){x.strokeStyle='#ff64d6';x.lineWidth=3;x.globalAlpha=.65+.25*Math.sin(run*12);x.beginPath();x.arc(p.x,p.y,p.r+9+Math.sin(run*9)*2,0,7);x.stroke();x.globalAlpha=1}x.globalAlpha=p.iv>0&&Math.floor(p.iv*20)%2?.4:1;x.save();x.translate(p.x,p.y);x.rotate(Math.hypot(...mv)>.1?Math.atan2(mv[1],mv[0]):-1.57);x.fillStyle=p.weapon==='scatter'?'#ffd19b':p.weapon==='rail'?'#e0baff':'#b9faff';x.shadowColor=p.weaponColor;x.shadowBlur=20;x.beginPath();x.moveTo(18,0);x.lineTo(-9,-10);x.lineTo(-5,0);x.lineTo(-9,10);x.closePath();x.fill();x.restore();x.globalAlpha=1;let w=Math.min(220,W*.5);x.fillStyle='#ffffff18';x.fillRect(14,H-18,w,6);x.fillStyle=p.hp/p.max>.3?'#73ebff':'#ff567a';x.fillRect(14,H-18,w*cl(p.hp/p.max,0,1),6);x.restore()}function loop(n){if(state==0||state==4)return;let dt=Math.min(.033,(n-last)/1000||.016);last=n;if(state==1)update(dt);draw();requestAnimationFrame(loop)}document.addEventListener('visibilitychange',()=>{if(document.hidden&&state==1)state=3;else if(!document.hidden&&state==3){state=1;last=performance.now();toast('RUN RESUMED')}});draw()})();</script></body></html>