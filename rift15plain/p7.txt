)+'s';autoColor='#ff8f9e'}else if(backlashT>0){autoMsg='BACKLASH · '+backlashT.toFixed(1)+'s';autoColor='#ff9d72'}else if(surgeT>0){autoMsg='RIFT SURGE · '+surgeT.toFixed(0)+'s';autoColor='#7adfff'}else if(strain()>=2){autoMsg='RELIC STRAIN '+strain();autoColor='#d6a4ff'}if(tl!==lastThreat){if(tl>lastThreat&&tl>0)signal('THREAT '+tl+' · '+threatName(tl),tl>=5?'#ff657f':tl>=3?'#ffba72':'#8ff3ff',2.4);lastThreat=tl}signalT=Math.max(0,signalT-dt);let showMsg=autoMsg||(signalT>0?signalMsg:'');U.sig.textContent=showMsg;U.sig.style.color=autoMsg?autoColor:signalColor;U.sig.classList.toggle('show',!!showMsg);U.st.textContent=autoMsg;U.ot.textContent=overT>0?overT.toFixed(1)+'s':overLock>0?'LOCK '+overLock.toFixed(1)+'s':overCharge>=100?'READY':Math.floor(overCharge)+'%';U.o.classList.toggle('ready',overCharge>=100&&overT<=0&&overLock<=0);U.o.classList.toggle('active',overT>0);U.o.classList.toggle('locked',overLock>0&&overT<=0);let bo=E.find(e=>e.type==='boss'&&!e.dead)||E.find(e=>e.type==='nemesis'&&!e.dead)||E.find(e=>e.type==='hunter'&&!e.dead);if(bo){U.boss.classList.add('show');U.bf.style.width=cl(bo.hp/bo.max*100,0,100)+'%';U.bn.textContent=bo.type==='hunter'?'RIFT STALKER':bo.type==='nemesis'?('RIFT NEMESIS'+(bo.phase?' · ADAPTED':'')):('RIFT WARDEN · '+(bo.mod||'STANDARD')+(bo.intro>0?' · ARRIVING':bo.rupture?' · RUPTURE':bo.enrage?' · ENRAGED':''))}else U.boss.classList.remove('show')}function draw(){x.setTransform(D,0,0,D,0,0);x.fillStyle=overT>0?'#12071a':surgeT>0?'#0b1420':'#061018';x.fillRect(0,0,W,H);x.save();if(shake)x.translate(R(-shake,shake),R(-shake,shake));x.strokeStyle='#59b8d20d';for(let a=run*5%42;a<W;a+=42){x.beginPath();x.moveTo(a,0);x.lineTo(a,H);x.stroke()}for(let q of Q){x.fillStyle='#9af8ff';x.shadowColor='#9af8ff';x.shadowBlur=12;x.beginPath();x.arc(q.x,q.y,5,0,7);x.fill()}x.shadowBlur=0;for(let a of A){let inside=ds(p,a)<a.r*a.r,urg=a.life<4;x.save();x.globalAlpha=.16+(inside?.08:0);x.fillStyle=urg?'#ff557d':'#65efff';x.beginPath();x.arc(a.x,a.y,a.r,0,7);x.fill();x.globalAlpha=.9;x.strokeStyle=urg?'#ff6688':'#79f5ff';x.lineWidth=2.5;x.beginPath();x.arc(a.x,a.y,a.r,0,7);x.stroke();x.strokeStyle='#e9ffff';x.lineWidth=5;x.beginPath();x.arc(a.x,a.y,a.r+7,-Math.PI/2,-Math.PI/2+6.283*(a.cap/a.need));x.stroke();x.fillStyle=urg?'#ff89a1':'#dffcff';x.font='700 10px system-ui';x.textAlign='center';x.fillText(a.cap>0?'HOLD '+(Math.ceil((a.need-a.cap)*10)/10)+'s':'ANCHOR · '+Math.ceil(a.life)+'s',a.x,a.y+3);x.restore()}for(let f of F){if(!f.boom){let a=.22+.28*(1-cl(f.t/1.45,0,1));x.globalAlpha=a+.16*Math.sin(run*14);x.fillStyle='#ff315522';x.strokeStyle='#ff5b72';x.lineWidth=2;x.beginPath();x.arc(f.x,f.y,f.r,0,7);x.fill();x.stroke();x.globalAlpha=1}else{x.globalAlpha=Math.max(0,f.l/.38);x.strokeStyle='#ffb0ba';x.lineWidth=8;x.beginPath();x.arc(f.x,f.y,f.r*(1.2-f.l),0,7);x.stroke();x.globalAlpha=1}}for(let b of B){x.strokeStyle=b.crit?'#ffe58a':(b.c||'#b7fbff');x.lineWidth=b.rail?4:b.crit?3:2;x.beginPath();x.moveTo(b.px,b.py);x.lineTo(b.x,b.y);x.stroke()}for(let b of EB){x.fillStyle=b.c;x.beginPath();x.arc(b.x,b.y,b.r,0,7);x.fill()}for(let e of E){if(e.type==='boss'&&e.intro>0){x.globalAlpha=.35+.25*Math.sin(run*12);x.strokeStyle='#e6a0ff';x.lineWidth=3;x.beginPath();x.arc(e.x,e.y,e.r+22+(1.45-e.intro)*18,0,7);x.stroke();x.globalAlpha=1}if(e.type==='boss'&&e.aimT>0){x.strokeStyle='#ff8bea99';x.lineWidth=3;x.setLineDash([10,7]);x.beginPath();x.moveTo(e.x,e.y);x.lineTo(e.x+Math.cos(e.aimA)*520,e.y+Math.sin(e.aimA)*520);x.stroke();x.setLineDash([])}if(e.type==='boss'&&e.novaT>0){x.globalAlpha=.30+.25*Math.sin(run*16);x.strokeStyle='#d876ff';x.lineWidth=4;x.beginPath();x.arc(e.x,e.y,e.r+38+(1-e.novaT)*42,0,7);x.stroke();x.globalAlpha=1}if(e.type==='lancer'&&e.wind>0){let aa=Math.atan2(p.y-e.y,p.x-e.x);x.strokeStyle='#ffcc6788';x.lineWidth=2;x.setLineDash([8,7]);x.beginPath();x.moveTo(e.x,e.y);x.lineTo(e.x+Math.cos(aa)*440,e.y+Math.sin(aa)*440);x.stroke();x.setLineDash([]);x.globalAlpha=.35+.35*Math.sin(run*18);x.strokeStyle='#ffe0a0';x.beginPath();x.arc(e.x,e.y,e.r+8,0,7);x.stroke();x.globalAlpha=1}if(e.type==='siphon'&&e.link){x.strokeStyle='#63ffb588';x.lineWidth=1.5;x.beginPath();x.moveTo(e.x,e.y);x.lineTo(p.x,p.y);x.stroke()}if((e.shield||0)>0){x.strokeStyle='#62d7ff';x.lineWidth=3;x.globalAlpha=.35+.35*(e.shield/e.maxShield);x.beginPath();x.arc(e.x,e.y,e.r+7,0,7);x.stroke();x.globalAlpha=1}if(e.elite){x.strokeStyle='#ffe28a';x.lineWidth=2;x.beginPath();x.arc(e.x,e.y,e.r+5,0,7);x.stroke()}if(e.type==='nemesis'){x.strokeStyle='#ff365f';x.lineWidth=2.5;x.globalAlpha=.45+.25*Math.sin(run*8);x.beginPath();x.arc(e.x,e.y,e.r+9,0,7);x.stroke();x.globalAlpha=1}x.fillStyle=e.hitT>0&&Math.floor(e.hitT*28)%2?'#ffffff':e.c;x.shadowColor=e.c;x.shadowBlur=e.type==='boss'?25:10;x.beginPath();x.arc(e.x,e.y,e.r,0,7);x.fill();x.fillStyle='#061018';x.beginPath();x.arc(e.x,e.y,e.r*.36,0,7);x.fill();x.shadowBlur=0;if(e.hitT>0&&e.type!=='boss'){let bw=e.r*2.2,bx=e.x-bw/2,by=e.y-e.r-10;x.fillStyle='#ffffff24';x.fillRect(bx,by,bw,3);x.fillStyle='#8af4ff';x.fillRect(bx,by,bw*cl(e.hp/e.max,0,1),3)}}for(let z of P){x.globalAlpha=Math.max(0,z.l/.45);x.fillStyle=z.c;x.beginPath();x.arc(z.x,z.y,2,0,7);x.fill()}if(p.relics.length){x.strokeStyle='#ba7cff';x.lineWidth=1.5;x.globalAlpha=.25+.12*Math.sin(run*5);x.beginPath();x.arc(p.x,p.y,p.r+14+p.relics.length,0,7);x.stroke();x.globalAlpha=1}if(overT>0){x.strokeStyle='#ff64d6';x.lineWidth=3;x.globalAlpha=.65+.25*Math.sin(run*12);x.beginPath();x.arc(p.x,p.y,p.r+9+Math.sin(run*9)*2,0,7);x.stroke();x.globalAlpha=1}x.globalAlpha=p.iv>0&&Math.floor(p.iv*20)%2?.4:1;x.save();x.translate(p.x,p.y);x.rotate(Math.hypot(...mv)>.1?Math.atan2(mv[1],mv[0]):-1.57);x.fillStyle=p.weapon==='scatter'?'#ffd19b':p.weapon==='rail'?'#e0baff':'#b9faff';x.shadowColor=p.weaponColor;x.shadowBlur=20;x.beginPath();x.moveTo(18,0);x.lineTo(-9,-10);x.lineTo(-5,0);x.lineTo(-9,10);x.closePath();x.fill();x.restore();x.globalAlpha=1;let w=Math.min(220,W*.5);x.fillStyle='#ffffff18';x.fillRect(14,H-18,w,6);x.fillStyle=p.hp/p.max>.3?'#73ebff':'#ff567a';x.fillRect(14,H-18,w*cl(p.hp/p.max,0,1),6);x.restore()}function loop(n){if(state==0||state==4)return;let dt=Math.min(.033,(n-last)/1000||.016);last=n;if(state==1)update(dt);draw();requestAnimationFrame(loop)}document.addEventListener('visibilitychange',()=>{if(document.hidden&&state==1)state=3;else if(!document.hidden&&state==3){state=1;last=performance.now();toast('RUN RESUMED')}});draw()})();</script></body></html>