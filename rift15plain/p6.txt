e.spd*dt;e.y+=Math.sin(a)*e.spd*dt}if(ds(p,e)<(p.r+e.r)**2){damage(e.dmg);if(e.type==='lancer'&&e.dash>0)e.dash=0}}function end(){state=4;let earned=Math.max(1,Math.floor(score/1800)+Math.floor(kills/12)+Math.floor(run/45)+breakBonusC+anchorBonusC),mxp=Math.max(1,Math.floor(kills/3)+Math.floor(run/10)+Math.floor(peakThreat/2)+(breakpoint?2:0)+directiveBonus),oldRank=masteryRank(p.weapon);mastery[p.weapon]=(mastery[p.weapon]||0)+mxp;masteryBest[p.weapon]=Math.max(masteryBest[p.weapon]||0,run);ss('rrmastery',JSON.stringify(mastery));ss('rrmasteryBest',JSON.stringify(masteryBest));let newRank=masteryRank(p.weapon);credits+=earned;best=Math.max(best,Math.floor(score));bestTime=Math.max(bestTime,run);bestKills=Math.max(bestKills,kills);ss('rrbest',best);ss('rrbesttime',bestTime);ss('rrbestkills',bestKills);ss('rrcredits',credits);meta();$('#final').textContent=Math.floor(score).toLocaleString();$('#stats').textContent=Math.floor(run)+'s survived · '+kills+' kills · max x'+maxCombo.toFixed(1)+' · '+p.weaponName+' · Core Mk '+p.level+' · '+overUses+' Overdrive ('+Math.round(overActive/Math.max(1,run)*100)+'% uptime) · '+p.relics.length+' Relics · Threat '+peakThreat+(strain()?' · Strain '+strain():'')+(p.ascension?' · '+p.ascension:'')+(breakpoint?' · BREAKPOINT CLEARED':'')+(directiveDone?' · DIRECTIVE COMPLETE':'')+(anchorsCaptured||anchorsLost?' · '+anchorsCaptured+' ANCHORS SECURED · '+anchorsLost+' RUPTURED':'');$('#earned').textContent='+'+earned+' RIFT CREDITS · +'+mxp+' '+p.weaponName+' XP'+(directiveDone?' · +3 DIRECTIVE':'')+(newRank>oldRank?' · MASTERY RANK '+newRank:'');$('#best2').textContent=best.toLocaleString();$('#over').classList.remove('hide')}function clean(a,f){let w=0;for(let v of a)if(f(v))a[w++]=v;a.length=w}function segHit(b,e){let vx=b.x-b.px,vy=b.y-b.py,wx=e.x-b.px,wy=e.y-b.py,d=vx*vx+vy*vy,t=d?cl((wx*vx+wy*vy)/d,0,1):0,dx=b.px+vx*t-e.x,dy=b.py+vy*t-e.y,r=(b.r||4)+e.r;return dx*dx+dy*dy<r*r}function update(dt){run+=dt;updateDirective();peakThreat=Math.max(peakThreat,threatLevel());score+=dt*(7+run*.05)*combo*(surgeT>0?1.45:1)*(1+threatLevel()*.08);shake=Math.max(0,shake-dt*35);p.iv=Math.max(0,p.iv-dt);if(overT>0)overActive+=dt;overT=Math.max(0,overT-dt);overLock=Math.max(0,overLock-dt);overSat=Math.max(0,overSat-dt*.72);if(overCharge>=100&&overT<=0&&overLock<=0){overReady+=dt;if(overReady>8)overCharge=Math.max(88,overCharge-dt*3)}else overReady=0;surgeT=Math.max(0,surgeT-dt);backlashT=Math.max(0,backlashT-dt);bossGrace=Math.max(0,bossGrace-dt);p.traumaT=Math.max(0,p.traumaT-dt);p.siphonT=Math.max(0,p.siphonT-dt);p.repairPool=Math.min(12,p.repairPool+dt*2.4);p.nullCd=Math.max(0,(p.nullCd||0)-dt);p.cd-=dt;if(hot>0)hot=Math.max(0,hot-dt*.35);else combo=Math.max(1,combo-dt*.28);if(p.dashes<p.maxDash){p.re+=dt;if(p.re>p.reTime){p.re=0;p.dashes++}}if(p.dashT>0){p.dashT-=dt;p.x+=p.vx*dt;p.y+=p.vy*dt;for(let e of E)if(!e.dead&&ds(p,e)<(p.r+e.r+8)**2)hit(e,95+p.level*6,1)}else{let sm=overT>0?1.18:1;p.x+=mv[0]*p.spd*sm*dt;p.y+=mv[1]*p.spd*sm*dt}p.x=cl(p.x,18,W-18);p.y=cl(p.y,55,H-18);if(p.cd<=0)fire();spawn-=dt;if(spawn<0){if(bossGrace>0){spawn=.18}else{let q=Math.random(),ty=run>120&&q<.06?'shade':run>55&&q<.12?'harrier':run>24&&q<.18?'lancer':run>80&&q<.25?'siphon':run>60&&q<.33?'aegis':run>95&&q<.40?'bomber':run>70&&q<.49?'sniper':run>50&&q<.59?'tank':run>35&&q<.69?'splitter':run>12&&q<.81?'spitter':'chaser';enemy(ty);spawn=Math.max(.19,.76-run*.0038)*R(.75,1.15)*(surgeT>0?.68:1)*(backlashT>0?.70:1)*(1-Math.min(.22,threatLevel()*.035))}}if(!ascended&&run>=ascendAt){ascended=1;ascensionChoice();return}if(!breakpoint&&run>=50){breakpoint=1;breaks++;breakBonusC=2;breakBonusX=2;score+=1200;ss('rrbreaks',breaks);meta();toast('BREAKPOINT CLEARED','50s · +2 CREDITS · +2 MASTERY BANKED');tone(1120,.15)}if(run>=anchorAt&&!A.length){anchorWave();anchorAt+=42}if(run>=fractureAt){fractureWave();fractureAt+=34}if(run>=surgeAt){surgeT=11;surgeAt+=75;toast('RIFT SURGE','11s · MORE THREATS · +35% OVERDRIVE CHARGE');shake=7}if(run>=hunterAt){enemy('hunter');hunterAt+=60;toast('HUNTER SIGNAL','RIFT STALKER INBOUND');shake=6}if(run>=nemesisAt&&threatLevel()>=3&&!E.some(e=>e.type==='nemesis'&&!e.dead)){enemy('nemesis');nemesisAt+=95;toast('NEMESIS LOCK','THE RIFT IS ADAPTING TO YOUR BUILD');shake=10}if(run>=bossAt){EB.length=0;bossGrace=2.35;enemy('boss');bossAt+=60;toast('WARDEN ARRIVAL','PROJECTILES PURGED · READ THE TELEGRAPHS');shake=8}for(let e of E)if(!e.dead){e.hitT=Math.max(0,(e.hitT||0)-dt);enemyUpdate(e,dt)}for(let b of B){b.px=b.x;b.py=b.y;b.x+=b.vx*dt;b.y+=b.vy*dt;b.l-=dt;for(let e of E)if(!e.dead&&b.l>0&&segHit(b,e)){hit(e,b.d);burst(b.x,b.y,b.crit?'#ffe58a':'#b7fbff',3);tone(b.crit?760:540,.025);if(--b.pi<0)b.l=0}}for(let b of EB){b.x+=b.vx*dt;b.y+=b.vy*dt;b.l-=dt;if(b.l>0&&ds(p,b)<(p.r+b.r)**2){b.l=0;damage(b.d)}}for(let a of A)anchorUpdate(a,dt);clean(A,a=>!a.dead);for(let f of F){if(!f.boom){f.t-=dt;if(f.t<=0){f.boom=1;f.l=.38;shake=Math.max(shake,8);burst(f.x,f.y,'#ff4d72',28);tone(115,.08);if(ds(p,f)<f.r*f.r)damage(18+threatLevel()*2.5);for(let e of E)if(!e.dead&&ds(e,f)<f.r*f.r)hit(e,70+threatLevel()*12,1)}}else f.l-=dt}clean(F,f=>!f.boom||f.l>0);for(let q of Q){q.x+=q.vx*dt;q.y+=q.vy*dt;q.vx*=.95;q.vy*=.95;let d=Math.sqrt(ds(p,q));if(d<p.mag){let a=Math.atan2(p.y-q.y,p.x-q.x),z=(1-d/p.mag)*550;q.vx+=Math.cos(a)*z*dt;q.vy+=Math.sin(a)*z*dt}if(d<24){q.dead=1;p.shards+=q.w;score+=20*q.w;if(p.shards>=p.next&&state==1)evolve()}}for(let z of P){z.x+=z.vx*dt;z.y+=z.vy*dt;z.vx*=.97;z.vy*=.97;z.l-=dt}clean(E,e=>!e.dead);clean(B,b=>b.l>0);clean(EB,b=>b.l>0);clean(Q,q=>!q.dead);clean(P,z=>z.l>0);if(P.length>420)P.splice(0,P.length-420);if(EB.length>260)EB.splice(0,EB.length-260);if(B.length>220)B.splice(0,B.length-220);U.s.textContent=Math.floor(score).toLocaleString();let tl=threatLevel();U.t.textContent=String(Math.floor(run/60)).padStart(2,'0')+':'+String(Math.floor(run%60)).padStart(2,'0')+' · T'+tl;U.h.style.width=(overT>0?100:overCharge)+'%';U.c.textContent='CORE '+p.level+' · '+p.shards+'/'+p.next;U.m.textContent=combo>1.8?'x'+combo.toFixed(1)+' COMBO':'';U.w.textContent=p.weaponName+' · MASTERY '+p.masteryRank+'/10';if(U.dir)U.dir.textContent=directiveText();U.d.textContent='●'.repeat(p.dashes)+'○'.repeat(p.maxDash-p.dashes);U.th.textContent='THREAT '+tl+' · '+threatName(tl);let autoMsg='',autoColor='#8ff3ff';if(p.siphonT>0){autoMsg='SIPHONED · REPAIRS SUPPRESSED';autoColor='#ff8f9e'}else if(p.traumaT>0){autoMsg='TRAUMA · '+p.traumaT.toFixed(1