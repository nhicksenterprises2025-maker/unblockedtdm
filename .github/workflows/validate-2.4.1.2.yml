name: Validate Skirmish Arena 2.4.1.2

on:
  push:
    branches: [hotfix/2.4.1.2-scoreboard-teams]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Run complete syntax and regression gates
        run: npm run check
